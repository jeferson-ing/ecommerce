<%= form_with(model: histroal, local: true) do |form| %>
  <% if histroal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(histroal.errors.count, "error") %> prohibited this histroal from being saved:</h2>

      <ul>
        <% histroal.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.number_field :order_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
